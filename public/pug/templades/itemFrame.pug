each r of files
    div(class=`item-img-sm m-2 ${admin === true ? 'item-admin' : ''}` id=`itemImg-${r.id}` data-id=(r.id))
        - href = board === boards.VIDEOS.path ? `${archiveUrl}/${r.thumb_name}` : (r.mime) ? `${archivePreviewUrl}/${r.preview}` : `${archiveUrl}/${r.filename}`
        img(src=(href) onerror=`this.src='${bdgePath}/assets/404.jpg'`)